apiVersion: v1
kind: Service
metadata:
  name: ecs-manager
  namespace: ecs-platform
  labels:
    app: ecs-manager
spec:
  type: ClusterIP
  selector:
    app: ecs-manager
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
